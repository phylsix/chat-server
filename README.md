# chat-server

A simple public chatroom that allow multiple clients connect and speak freely.

## Set up

- Terminal 0: start chat server with `cargo run`
- Terminal 1..n: join chat room with `telnet localhost 8080`